###
# work specific options
###


set from      = "ziltoid@internode.on.net"
set sendmail  = "/usr/bin/msmtp -a cca"
set mbox      = "+Node/INBOX"
set record    = "+Node/Sent"
set postponed = "+Node/Draft"
set signature = "fortune|"
color status cyan default

macro index ,c "<change-folder>+GMail/INBOX<return>" "change accounts"
macro index D "<save-message>+Node/Trash<enter>" "move message to the trash"
macro index S "<save-message>+Node/Spam<enter>"  "mark message as spam"
